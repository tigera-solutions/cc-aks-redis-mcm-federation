K8S_CONTEXTS=("aks-kartik-cc-mcm-redis-westus" "aks-kartik-cc-mcm-redis-canadacentral")

# You don't need to change the following variables values.
# Make sure you know what you are doing if you change this.
NAMESPACE=redis
INSTALL_FILES=( "my-rec-clustera.yaml" "my-rec-clusterb.yaml" )